mixin videoPlayer(video ={}) 
    .videoPlayer 
        video(src=video.src)
        .videoPlayer__controls 
            .videoPlayer__column
            .videoPlayer__column
            .videoPlayer__column